#ifndef UTIL_H
#define UTIL_H


// Seeds the pseudo-random number generator
void initRand();


// Generates a random integer in a range (inclusive)
int randInt(const int&, const int&);


// Generates a random float in a range
float randFloat(const float&, const float&);


#endif

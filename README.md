# Bouncing Balls
This program renders a window with balls bouncing around.

## Prerequisites
The following libraries are required to compile:
- g++
    - Debian-based distributions: `g++`
    - Red Hat-based distributions: `gcc-c++`
- SDL2
    - Debian-based distributions: `libsdl2-dev`
    - Red Hat-based distributions: `SDL2-devel`

## Usage
Execute the following commands in the project directory to compile and run:
```
make
./bin/bouncing_balls
```
